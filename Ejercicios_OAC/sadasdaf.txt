%include "./pc_io.inc" 
section .data
section .bss
section .text
global sumalista:

imprimirPuts: pushad

    push EBP
    mov ebp,esp
    mov esi, [ebp+8]
    ;mov ecx, [ebp+12]
    mov eax,0
    mov edi,0

    .continue:
        mov edx, [ebp+8]
        call puts
    
    ;loop .continue
    
    mov esp,ebp 
    pop EBP
    popad
    ret



#include <stdio.h>
extern int imprimirPuts(int * arreglo);

int main(void){

    int arreglo[20];
    int tamanio;
    int bandera=0;
    int i=0;

    while(bandera !=0){
        if (arreglo[i] == '*'){
            banderra++;
        }
        scanf("%d",arreglo[i]);
        i++;
    }
    int muestra= imprimirPuts(arreglo);
    printf("Mostrar: %d\n",muestra);
    return 0;

}